--all::
Alias for `--monitor all`.
Use it only when you genuinely need workspaces/windows from *all monitors*.
+
In a multi-monitor setup `--monitor focused` is almost always the option you want.
A script that operates on all monitors will act on workspaces and windows you were not looking at.
