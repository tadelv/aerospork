The only explanatory-text style in the product. Use as a Form section footer, inside a ListActionBar, or under a composer. Backtick spans in a string child render monospaced.

~~~jsx
<SettingsHint>Auto gives wide monitors a horizontal split and tall monitors a vertical one.</SettingsHint>
~~~

Full sentences, no exclamation marks; explain the consequence rather than restating the control.
