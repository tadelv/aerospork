Use for any pinned toolbar, path bar, composer or footer. Never stack two bar strips — merge their content into one.

~~~jsx
<BarStrip edge="top"><TextField variant="plain" placeholder="Filter" /></BarStrip>
<BarStrip><Button variant="prominent">Apply</Button></BarStrip>
~~~
