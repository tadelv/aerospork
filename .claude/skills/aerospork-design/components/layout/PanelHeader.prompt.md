Use as the header of any pane that isn't a `FormSection` — a list pane, a split-view list, a bare `VStack`. `SectionLabel` pre-wrapped with the padding a Form section header gets for free from `.formStyle(.grouped)`: horizontal 16, top 14, bottom 8.

~~~jsx
<PanelHeader title="Connected monitors" sf="display.2" />
~~~
