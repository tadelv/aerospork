Put next to any value a user has to paste somewhere else — display UUIDs, config paths, version strings for a bug report.

```jsx
<CopyButton value={uuid} help={`Copy display UUID\n${uuid}`} />
```

Always pairs with the visible value; never the only way to read it.
